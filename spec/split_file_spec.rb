require "spec_helper"

describe SplitFile do
  it "has a version number" do
    expect(SplitFile::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
